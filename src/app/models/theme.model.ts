export class Theme {
  _id: string;

  title: string;
  date: Date;
  description: string;

  totalPosts: number;
  totalViews: number;
  userId: string;
  forumId: string;

  // title: string;
  // date: Date;
  // forumId: string;

  constructor(){}
}
